services:
  mongodb:
    image: mongo:7
    container_name: contractiq-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - contractiq-mongo-data:/data/db

volumes:
  contractiq-mongo-data: